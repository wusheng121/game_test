# ============================================================
# Godot 4 .gitignore
# ============================================================
# Godot 在打开项目时会自动生成一些缓存与导入文件，
# 这些不应该提交到 git，因为：
#   1. 每个用户/平台的版本可能不同
#   2. 体积大且可重新生成
#   3. 提交后会造成频繁冲突

# --- Godot 编辑器缓存目录 ---
# 包含 .godot/import/ 下的导入产物、editor 临时状态等
.godot/

# --- 编辑器用户配置 ---
# 这些是个人偏好（如最近打开的项目列表）
/editor/

# --- Android 导出构建产物 ---
/android/build/

# --- macOS / Windows / Linux 系统垃圾文件 ---
.DS_Store
Thumbs.db
*.tmp
*.bak
*.swp

# --- 编辑器配置（IDE 个性化） ---
.vscode/
.idea/
*.code-workspace

# --- Godot 启动时生成的日志 ---
*.log

# --- 导出产物（如果你的游戏导出到项目目录里） ---
# 取消注释下面的行如果你不希望提交编译后的游戏：
# /builds/
# *.exe
# *.app
# *.x86_64
